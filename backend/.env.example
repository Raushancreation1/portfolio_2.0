# Server
PORT=4000
NODE_ENV=development

# CORS: comma-separated list of allowed frontend origins
# Example for local dev with Vite and a deployed site:
# CORS_ORIGINS=http://localhost:5173,https://your-frontend.example.com
CORS_ORIGINS=

# Contact form (optional for future email integration)
CONTACT_TO_EMAIL= 

# Database
# Example for local MongoDB
MONGODB_URI=mongodb+srv://raushancreationofficial_db_user:CMZZ0zA3QW7S1w4U@portfolio.jyfdnvr.mongodb.net
MONGODB_DB=portfolio

# Example for MongoDB Atlas (uncomment and fill in)
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/<db-name>?retryWrites=true&w=majority

# AI Chatbot (optional)
# Obtain from https://platform.openai.com/ and set here. If not set, the chatbot will echo messages.
# OPENAI_API_KEY=

# Google Gemini (optional)
# Obtain an API key from https://ai.google.dev/ and set here to use Gemini models.
# Do NOT commit real keys. Put real keys only in backend/.env (which is gitignored).
# GEMINI_API_KEY=
 
 GEMINI_API_KEY= AIzaSyDL8yik_nf7zC_MY9lR8PZYC4eFMMjYtEw